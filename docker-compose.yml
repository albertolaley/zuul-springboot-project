version: '2'
services:
  customerservice:
    build: customerservice
    mem_limit: 512M
    ports:
      - "8181:8181"
    environment:
      - JAVA_OPTS=
         -DEUREKA_URI=http://registryservice:8761/eureka
